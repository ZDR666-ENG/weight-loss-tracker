[build]
  command = "npx prisma generate && npm run build && LINK=$(ls -d .next/node_modules/@prisma/client-* 2>/dev/null | head -1) && rm -rf \"$LINK\" && cp -r node_modules/@prisma/client \"$LINK\" && cp -r node_modules/.prisma \"$LINK/.prisma\""
  publish = ".next"
